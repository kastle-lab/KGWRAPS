# Paper Downloader
`download_metadata.py` works
`downloader.py` needs work to fit with the metadata objects `download_metadata.py` saves.

Read through the files, they should explain enough.

I have 59GB of metadata on Arsenal.